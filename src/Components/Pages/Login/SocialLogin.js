import React from 'react';

const SocialLogin = () => {
    return (
        <div>
            <div class="divider">OR</div>
            <button class="btn btn-active btn-accent w-full">Continue With Google</button>
        </div>
    );
};

export default SocialLogin;